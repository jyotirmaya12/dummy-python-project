machine:
  python:
    version: 2.6.6
test:
  pre:
    - aws --version
    - aws s3 --version
  override:
    - python -v
    - coverage run test.py
  post:
    - coverage html -d $CIRCLE_ARTIFACTS/coverage

