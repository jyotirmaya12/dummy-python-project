machine:
  python:
    version: 2.7.5

test:
  pre:
    - ls -la ~/.ssh
  override:
    - echo "passing":
        timeout: 600
    - echo "running on $CIRCLE_NODE"
        parallel: true
    - ./slow-output.py


deploy:
  foobar:
    branch: [.*]
    commands:
      - echo "running on $CIRCLE_NODE"
