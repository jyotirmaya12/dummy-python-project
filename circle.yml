machine:
  python:
    version: 2.7.5

deployment:
  foobar:
    branch: master
    commands:
      - ls -l ~/.netrc
      - cat ~/.netrc | ./redacter.py
      - heroku run date --app dummy-heroku-app
      - echo "running in parallel?":
          parallel: true
