machine:
  python:
    version: 2.7.5

test:
  pre:
    - ls -la ~/.ssh
  override:
    - echo "passing":
        timeout: 600
    - ./slow-output.py


deploy:
  foobar:
    branch: master
    commands:
      - echo "running on $CIRCLE_NODE"
          parallel: true
