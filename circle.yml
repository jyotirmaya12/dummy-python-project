test:
  override:
    - case $CIRCLE_NODE_INDEX in 0) coverage run -p test_a.py ;; 1) coverage run -p test_b.py ;; esac:
        parallel: true
    - scp node1:./$CIRCLE_PROJECT_REPONAME/.coverage* .
    - coverage combine
    - coverage report
    - coverage html -d $CIRCLE_ARTIFACTS/htmlcov

