machine:
  python:
    version: 3.3.2
  environment:
    FOO: $HOME
    BAR: $FOO-bar

test:
  override:
    - echo "force good"
  post:
    - python --version
    - pip --version

deployment:
  testing:
    branch: python3
    commands:
      - python requests-test.py


